import React from "react";
import { Typography, Box } from "@mui/material";

export default function TlsmOicDashboard() {
  return (
    <Box textAlign="center">
      <Typography variant="h4">TL / SM / OIC Dashboard</Typography>
      <Typography sx={{ mt: 2 }}>Project-based access dashboard.</Typography>
    </Box>
  );
}
